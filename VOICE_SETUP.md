# MS Voice

MS uses `en-IN-NeerjaNeural` through edge-tts when internet is available. If neural TTS fails, Windows female SAPI is used automatically.

Run from VS Code:
`python main.py`

No TTS API key is required. Gemini API remains configured separately through `GEMINI_API_KEY`.
